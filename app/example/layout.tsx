export default function({ children }: { children: React.ReactNode }) {
    return (
        <section>
            Example Layout 
            {children}
            example Footer
        </section>
    );
}